export class Bubble { constructor(random=true){this.reset(random);} reset(random=false){this.x=random?Math.random():.08+Math.random()*.84;this.y=random?Math.random():1.03;this.r=.003+Math.random()*.009;this.speed=.03+Math.random()*.06;this.phase=Math.random()*7;} update(dt,flow){this.y-=this.speed*dt;this.x+=Math.sin(this.y*12+this.phase)*dt*.012*flow;if(this.y<-.04)this.reset();} draw(ctx,w,h){ctx.beginPath();ctx.strokeStyle='#d7fbff9a';ctx.lineWidth=Math.max(1,this.r*w*.18);ctx.arc(this.x*w,this.y*h,this.r*w,0,Math.PI*2);ctx.stroke();} }
